import styled from "@emotion/styled";

const Archive = () => {
  return <ArchiveContainer>Archive</ArchiveContainer>;
};

const ArchiveContainer = styled.div`
  text-transform: uppercase;
`;

export default Archive;
