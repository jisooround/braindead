import styled from "@emotion/styled";

const SpecialProjects = () => {
  return <SpecialContainer>SpecialProjects</SpecialContainer>;
};

const SpecialContainer = styled.div`
  text-transform: uppercase;
`;

export default SpecialProjects;
